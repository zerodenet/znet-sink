# 2026-09-05 RC 构建记录

本次从两个仓库的 `main` 发布，分支与不可变 RC 标签均同步到 GitLab `origin` 和 GitHub 镜像。

| 项目 | RC 版本 | 发布提交 | 功能提交 |
| --- | --- | --- | --- |
| ZNet Sink | `0.0.16-rc.202609050729` | `3bc82aaaff35965f6d38aef13ad78026148f98b7` | `10b342bf4d94d4b752242d5e08608184611bab53` |
| Zero | `0.0.16-rc.202609050728` | `500049a851b3e3339520488606892fb590de97cb` | `03ce5bdf12d9a7baefd8e3365b5c544d76b6ef7f` |

客户端原生构建号为 `349`。两个 RC 的时间戳按各自发布脚本生成，属于同一轮配套构建。

## 变更

- 客户端配置原子保存、备份恢复；内核就绪校验、连接操作串行保护；内核升级备份与错误回退。
- 版本管理展示安装阶段、保留失败消息并支持重试，修正更新版本比较和重复路径保存；同版本重装修复执行权限。
- 包含工作区原有的 TUN CIDR/mask 归一化和 RC 历史保留策略调整。
- 内核支持空配置管理待命、首次监听应用及失败重试；行为变更已登记到内核兼容性记录。

## 远端构建

- [客户端 Release 工作流](https://github.com/zerodenet/znet-sink/actions/runs/33952759301)
- [内核 Release 工作流](https://github.com/zerodenet/core/actions/runs/33952707851)

客户端工作流负责 Windows、macOS Intel/Apple Silicon、Linux 构建和 updater 签名验证，全部平台构建完成后才发布 Release。内核工作流先验证标签及质量，再构建发布产物。实际完成状态以工作流为准。

## 验证基线

功能提交之前已完成前端完整验证、GUI 后端 435 项测试、Chrome 12 项浏览器测试及包级隔离生命周期验收。内核工作区 1366 项测试通过，使用与 CI 相同的 16 MiB 测试线程栈。本次发布脚本再次检查版本元数据、兼容性记录及提交钩子。

本次 RC 已重新构建客户端和内核，DMG 完整性与原生构建号校验通过。使用这对 RC 的隔离包复验通过：首次启动、备份恢复、精确子进程 PID、两次强退 GUI 后内核退出；系统代理前后一致。内核远端全特性 Clippy 和工作区测试亦已通过。

历史无新版本号的候选验收见[第一轮](./production-candidate-20260905.md)和[第二轮](./first-release-polish-20260905.md)。

## 本地构建产物

产物不纳入 Git。客户端位于 GUI 工作区 `src-tauri/target/rc-20260905/`，内核位于 Zero 工作区 `target/rc-20260905/`。客户端产物目录同时保留 `manifest.json`、`SHA256SUMS`、构建日志、发布日志和 `smoke.log`。

| 文件 | 字节数 | SHA256 |
| --- | ---: | --- |
| `ZNet Sink_0.0.16-rc.202609050729_x64.dmg` | 14917970 | `b9c04860dea55cef2bf60f71c169e50c6990562e370d0c0b088085c515283c12` |
| `zero-0.0.16-rc.202609050728-darwin-x86_64.tar.gz` | 12273094 | `15dd4d0b9cf9aa436e6f129b8bea867d66c94f79e4dd05a22ddde598e51786d0` |

本地内核归档单独交付，未嵌入客户端 DMG。远端构建使用各平台独立环境，不能用本地 SHA256 替代远端产物的校验值。

## 边界

本地构建为未签名、未公证的 macOS Intel 包；正式 updater 产物由远端流程生成。本次不覆盖正在运行的已安装实例。升级过程中强退或断电后的 pending 备份仍需人工恢复，真实特权网络切换与各平台实机体验仍需另行验收。
